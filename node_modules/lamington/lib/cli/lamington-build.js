"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
const configManager_1 = require("../configManager");
/**
 * Executes a contract build procedure
 * @note Keep alive setup is incomplete
 * @author Kevin Brown <github.com/thekevinbrown>
 * @author Mitch Pierias <github.com/MitchPierias>
 */
const run = () => __awaiter(void 0, void 0, void 0, function* () {
    // Capture CLI defined contract identifiers
    const contract = process.argv[2];
    // Initialize Lamington configuration
    yield configManager_1.ConfigManager.initWithDefaults();
    // Start the EOSIO container image if it's not running.
    if (!(yield utils_1.eosIsReady())) {
        yield utils_1.startEos();
    }
    // Build all smart contracts
    yield utils_1.buildAll([contract]);
    // And stop it if we don't have keepAlive set.
    if (!configManager_1.ConfigManager.keepAlive) {
        yield utils_1.stopContainer();
    }
});
run().catch((error) => __awaiter(void 0, void 0, void 0, function* () {
    process.exitCode = 1;
    console.log(error);
    if (!configManager_1.ConfigManager.keepAlive && (yield utils_1.eosIsReady())) {
        yield utils_1.stopContainer();
    }
}));
